# Users Module — Placeholder

This directory is reserved for the users module implementation.

Implementation belongs to **SAN-6** (user creation and registration) and **SAN-8** (RBAC, profile/admin endpoints).

Files to be added here:
- `users.router.ts`
- `users.controller.ts`
- `users.service.ts`
