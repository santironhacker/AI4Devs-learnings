# Auth Module — Placeholder

This directory is reserved for the auth module implementation.

Implementation belongs to **SAN-7** (login, logout, session handling) and **SAN-6** (user registration).

Files to be added here:
- `auth.router.ts`
- `auth.controller.ts`
- `auth.service.ts`
- `lockout.ts` — in-memory failed-attempt tracker
