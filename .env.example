# Template for all required env vars (see docs/architecture.md §7)
# Copy to backend/.env (development) and fill in real values.

# Database
DATABASE_URL="postgresql://user:pass@localhost:5432/superusers"
